# Only needed for previewing locally (`bundle exec jekyll serve`).
# GitHub Pages builds the site itself and ignores this file's dev tooling.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
